{
  "name": "TV_Films",
  "version": "0.0.0",
  "description": "watch and add video files",
  "main": "index.js",
  "scripts": {
    "createDB": "sudo mysql -u root -p < server/createDB.sql",
    "start": "node server/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "charlie",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
